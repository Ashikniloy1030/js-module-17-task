function kullu(num1, num2) {
    const sum = num1 + num2;
    const diff = num1 - num2;
    const multi = sum * diff;
    const vag = multi / 2;
    return vag;

}
const result = kullu(30, 20);
console.log(result)
